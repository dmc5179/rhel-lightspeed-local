apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: role-oracle
  namespace: usertest
rules:
- apiGroups:
  - security.openshift.io 
  resourceNames:
  - scc-oraclt
  resources:
  - securitycontextconstraints 
  verbs: 
  - use
